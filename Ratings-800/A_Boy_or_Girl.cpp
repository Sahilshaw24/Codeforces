#include<bits/stdc++.h>
using namespace std;
 
int main(){
    string s;
    cin >> s;
    set<int> st;
    for(auto it : s){
        stt.insert(it);
    }
    if((st.size()) % 2 == 0) cout << "CHAT WITH HER!";
    else cout << "IGNORE HIM!";
    return 0;
}